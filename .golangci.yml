issues:
  max-issues-per-linter: 0
  max-same-issues: 0

linters:
  disable:
    - errcheck
    - unused
  enable:
    - errorlint
    - gofumpt
    - goimports
    - govet
    - revive
    - staticcheck

linters-settings:
  goimports:
    local-prefixes: github.com/prometheus/promu
